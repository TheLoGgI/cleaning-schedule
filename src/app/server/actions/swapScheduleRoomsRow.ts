"use server"

import { NextRequest } from "next/server"

export async function swapScheduleRoomsRow({ scheduleId, room1, room2 }) {
  console.log("prevState: ", prevState)
  console.log("formData: ", formData)
}
